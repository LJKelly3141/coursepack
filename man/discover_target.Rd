% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a11y-targets.R
\name{discover_target}
\alias{discover_target}
\title{Discover what an audit should look at in a rendered repository}
\usage{
discover_target(repo_path)
}
\arguments{
\item{repo_path}{Path to the repository to audit. Must exist.}
}
\value{
A list of \code{repo}, \code{framework} (\code{"quarto"}, \code{"bookdown"},
\code{"static"}, or \code{"unknown"}), \code{output_dir} (\code{NA} when it cannot be placed
confidently), \code{source_dir}, \code{pages}, \code{resource_dirs},
\code{unproduced_sources}, and \code{stylesheet}.
}
\description{
Takes a repository path and answers, from that project's own config and
layout, what built it, where the HTML went, which of those files are real
pages, which directories it only copies, which sources render to nothing,
and whether there is an authored stylesheet to put shared fixes into.
}
\details{
Nothing here defaults to any one course's path; the repository is always an
argument. A missing stylesheet is reported as \code{NA} rather than guessed at,
because a shared fix routed into a vendored or generated file is silently
overwritten by the next build.
}
\examples{
# A small rendered Quarto book: config, an authored theme file, and the
# output directory that config points at.
repo <- tempfile("repo-")
dir.create(file.path(repo, "docs"), recursive = TRUE)
writeLines(c("project:", "  type: book", "  output-dir: docs",
             "format:", "  html:", "    theme: [cosmo, custom.scss]"),
           file.path(repo, "_quarto.yml"))
writeLines("/* authored overrides */", file.path(repo, "custom.scss"))
writeLines("<html lang='en'></html>", file.path(repo, "docs", "index.html"))
writeLines("<html lang='en'></html>", file.path(repo, "docs", "week1.html"))

tgt <- discover_target(repo)
tgt$framework
tgt$pages
basename(tgt$stylesheet)   # NA here would mean "create one first"

unlink(repo, recursive = TRUE)
}
