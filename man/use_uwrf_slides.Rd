% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slides.R
\name{use_uwrf_slides}
\alias{use_uwrf_slides}
\title{Install the UWRF slide style into a course}
\usage{
use_uwrf_slides(proj, dir = "slides", overwrite = FALSE)
}
\arguments{
\item{proj}{Course project root. Written to under \code{dir}.}

\item{dir}{Directory under \code{proj} that holds the deck sources, created if it
does not exist. Quarto looks for \verb{_extensions/} beside a document, so the
decks and the extension share a directory.}

\item{overwrite}{Replace an installed copy. The default refuses, so a local
edit to the style is not lost by a re-run; pass \code{TRUE} to pick up a newer
version of the style from a reinstalled package.}
}
\value{
The path of the installed extension, \verb{<proj>/<dir>/_extensions/uwrf},
invisibly.
}
\description{
Copy the \code{uwrf} Quarto format extension into \code{dir} under the course root, so
a deck there can declare \code{format: uwrf-revealjs} and get the university
brand: Passion Red Impact headings, Arial body text, the UWRF logo, slide
numbers, linear navigation, a 1280 by 720 canvas and embedded resources. It
is the only slide style the package carries so far.
}
\details{
Nothing outside \code{proj} is touched, and the course's \verb{_quarto.yml} is not
edited: its render list is an explicit allowlist, and adding the deck
directory to it is the course's decision, so the closing summary says what
to add.
}
\examples{
p <- file.path(tempdir(), "uwrf-example")
init_course(p, code = "ABCD 101", title = "A Course",
            site_url = "https://example.invalid/c", timezone = "UTC",
            ask = FALSE, git = FALSE)
use_uwrf_slides(p)
list.files(file.path(p, "slides", "_extensions", "uwrf"))
unlink(p, recursive = TRUE)
}
