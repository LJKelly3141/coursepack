% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a11y-model.R
\name{finding}
\alias{finding}
\title{One accessibility finding}
\usage{
finding(
  surface,
  file,
  criterion,
  level,
  issue,
  selector,
  severity,
  detail = "",
  source_file = NA_character_,
  line = NA_integer_,
  source = "pa11y",
  count = 1L,
  code = NA_character_
)
}
\arguments{
\item{surface}{The audited surface's name, e.g. a repository or cartridge.}

\item{file}{The file the finding is in, relative to that surface.}

\item{criterion}{The WCAG success criterion number, or \code{NA} when the
producing engine gives none.}

\item{level}{The criterion's conformance level, or \code{NA} when it cannot be
derived from what the tool reported.}

\item{issue}{The message a person reads.}

\item{selector}{A CSS selector for the element, or \code{NA}.}

\item{severity}{\code{"serious"}, \code{"moderate"}, \code{"minor"}, or \code{"review"}.}

\item{detail}{Supporting context, e.g. the HTML snippet.}

\item{source_file}{The authored source file, when a rendered finding can
be traced back to one.}

\item{line}{The line in that source file.}

\item{source}{Which producer this came from, e.g. \code{"pa11y"} or
\code{"custom"}.}

\item{count}{How many instances the tool could not tell apart collapsed
into this row. Never hashed into the id.}

\item{code}{The producing check's own identifier, hashed into the id so
two different checks on one element stay two findings.}
}
\value{
A one-row data frame with the \code{FINDING_COLS} columns.
}
\description{
The record every check and every engine produces, and the row the JSON
report is written from. The id hashes surface, file, criterion, selector,
and the tool's own check code, and deliberately not the line number, the
HTML context, or the message text, so a re-audit can tell "the same
problem, still here" from "a new problem" across edits that move lines.
}
