% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init.R
\name{init_course}
\alias{init_course}
\title{Scaffold a course repository}
\usage{
init_course(
  path,
  code,
  title,
  site_url,
  timezone,
  institution = NULL,
  textbook_url = NULL,
  textbook_docs = "none",
  skills = TRUE,
  claude_md = TRUE,
  git = TRUE,
  from_export = NULL,
  ask = interactive()
)
}
\arguments{
\item{path}{Directory to scaffold into. Must not exist, or be empty.}

\item{code}{Course code, for example \code{"ABCD 101"}. The \verb{slug:} is derived
from it and every derived Canvas identifier from that.}

\item{title}{Course title.}

\item{site_url}{Where the rendered site is published. Must start with
\verb{https://}.}

\item{timezone}{IANA zone name, validated against \code{OlsonNames()}.}

\item{institution}{Optional institution name.}

\item{textbook_url}{Optional base URL of a separate textbook site, which a
\code{{textbook}} token in \code{modules.yml} resolves against.}

\item{textbook_docs}{A checkout of the rendered textbook, or \code{"none"}.}

\item{skills}{Whether to install the shipped skills under \verb{.claude/skills/}.}

\item{claude_md}{Whether to write the course's \code{CLAUDE.md}.}

\item{git}{Whether to run \verb{git init} in \code{path}. Nothing is committed, ever.}

\item{from_export}{Optional path to a Canvas \code{.imscc} to start from.}

\item{ask}{Whether to prompt for a missing required value. Prompts only when
this is \code{TRUE} and the session is interactive.}
}
\value{
\code{path}, invisibly.
}
\description{
Write a new course under \code{path}: the two manifests, the containment
scaffold, the Makefile whose every target calls a function in this package,
and, optionally, the skills. With \code{from_export} it starts from a Canvas
export instead of from placeholders.
}
\details{
Nothing here is guessed. \code{code}, \code{title}, \code{site_url} and \code{timezone} have no
defaults, and a missing one stops unless \code{ask} is on and the session is
interactive, in which case it is prompted for. \code{Sys.timezone()} is shown
beside the timezone prompt as a suggestion and is never used silently: a
wrong zone moves every deadline and every announcement by hours and looks
correct in the generated XML.

No date is written. \verb{term: first_day:} and \verb{last_day:} are \code{null}, because
those come from the registrar's calendar and counting weeks is how a course
ends up with a deadline on a day it does not meet. The scaffold still builds:
its one announcement posts on import, and a term window is required only for
an announcement that posts on a date.

\code{path} must not exist or must be empty, and nothing is ever overwritten.
}
\section{Starting from a Canvas export}{

\code{from_export} names an \code{.imscc}. The scaffold is written first, then
\code{\link[=extract_manifest]{extract_manifest()}} replaces \code{course.yml}, \code{modules.yml} and
\code{reference.yml} with the export's own structure and copies the export under
\verb{reference/}. The caller's \code{code}, \code{title}, \code{site_url}, \code{timezone},
\code{textbook_docs} and the template's \code{due_time} are then written back into the
extracted \code{course.yml}, because an export carries none of them.

The zone is the one to be careful with. The extractor writes every carried
\verb{due:} as the UTC instant Canvas stored and \code{term: timezone: UTC} above them,
so those two lines belong to each other. Naming a different zone here moves
every carried deadline by the offset between them, and the extracted \verb{due:}
dates have to be rewritten to match. The closing summary says so when it
applies.
}

