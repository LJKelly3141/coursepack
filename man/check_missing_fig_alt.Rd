% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a11y-checks.R
\name{check_missing_fig_alt}
\alias{check_missing_fig_alt}
\title{Flag figure-producing chunks that carry no fig-alt}
\usage{
check_missing_fig_alt(repo, surface, exclude)
}
\arguments{
\item{repo}{Repository to scan.}

\item{surface}{Surface these findings belong to.}

\item{exclude}{Repo-relative paths this project only copies rather than
authors, from \code{discover_target()}'s \code{resource_dirs} and
\code{unproduced_sources}. Required and has no default: a default would let a
caller silently scan a course's working files and still look correct.
Pass \code{character(0)} to scan everything, deliberately.}
}
\value{
A findings data frame, with \code{modes} and \code{mode} attributes. A repo
with no \code{.qmd} or \code{.Rmd} sources at all yields one loud row saying the
check examined nothing, never an empty frame.
}
\description{
Detection keys on whether a chunk ACTUALLY RENDERS AN IMAGE, checked two
ways. In \code{rendered} mode the chunk's real PNGs are enumerated from the
\verb{_files/figure-html} directory Quarto wrote and mapped back to source
chunks by name. In \code{source-heuristic} mode, used when nothing was rendered,
a plotting-call scan stands in. The result's \code{mode} attribute says which
ran, per file in \code{modes} and overall in \code{mode}, so a caller can always tell
verified figures from guessed ones.
}
