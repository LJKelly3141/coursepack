% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert.R
\name{convert_python_manifests}
\alias{convert_python_manifests}
\title{Convert a course from the Python manifest schema}
\usage{
convert_python_manifests(
  course_py,
  modules_py,
  out_dir,
  bank_dir = "questions"
)
}
\arguments{
\item{course_py}{Path to the Python schema's \code{course.yml}.}

\item{modules_py}{Path to the Python schema's \code{modules.yml}.}

\item{out_dir}{Directory the converted \code{course.yml} and \code{modules.yml} are
written into. Created if absent.}

\item{bank_dir}{The question bank directory, relative to the course project
root, written as \verb{dir:} in every converted \verb{bank:} block.}
}
\value{
\code{out_dir}, invisibly.
}
\description{
Read the \code{course.yml} and \code{modules.yml} of the toolchain this package
replaces and write the same course under \code{out_dir} in the package schema.
Run once per course, by hand, in that course's own session; the converted
files are then the course's own and are hand-edited from there.
}
\section{What changes shape}{

\verb{course_code:} becomes both \verb{code:} and \verb{canvas: course_code:}. \verb{urls:},
\verb{term:} and \verb{due_time:} travel unchanged, and \code{textbook_docs: none} is
written because the Python schema had no such key. The \verb{canvas:} block is
filtered through \code{CANVAS_SETTINGS_KEYS}; a key that is not a Canvas setting
stops. Assignment groups keep their identifiers, and a group that named none
is given the one the Python generator derived, because a carried quiz or
assignment names its group by identifier inside its own bytes.

The title-keyed \verb{due_dates:} map dissolves: each entry becomes the \verb{due:} of
the definition whose title it named, and an entry naming no definition stops
rather than being ignored. \verb{exam_pools:} is dropped, with a printed note when
it holds anything, because exams are generated from \verb{bank:} blocks here.
\verb{canvas: course_image:} is not a Canvas setting and is not written; the
converter prints where to put the file instead.

Every module item becomes an item naming a definition plus the definition
itself. A wiki page with a \verb{url:} becomes an \verb{iframe:} page; one marked
\verb{carry_through:} becomes a \verb{source_ref:} page. A quiz or assignment with a
\verb{generate:} block becomes a \verb{bank:} quiz or a \verb{description:} assignment; one
without becomes \verb{source_ref:}. A generated quiz that named no group is given
\verb{Module Quizzes}, which is the default the Python generator applied without
writing it down; a generated assignment that names no group stops, as it did
there. Definition ids are the slugified title.
}

\section{What it refuses}{

Module positions that do not strictly increase across the file, item
positions that do not strictly increase within a module, two definitions of
one kind claiming one title, a \code{due_dates} key matching no definition, an
item type it cannot write, a \verb{generate:} key it does not read, and a page
with neither a \verb{url:} nor \verb{carry_through:}. All of them before anything is
written.

Positions themselves are not written: in the package schema a module's and an
item's position is its place in the file. Where a declared position and that
place disagree, which is what a merged module leaves behind, the converter
prints \verb{positions renumbered: <old> -> <new>} so the change is on the record.
}

