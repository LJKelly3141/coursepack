% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skills.R
\name{install_skills}
\alias{install_skills}
\title{Install the shipped skills into a course}
\usage{
install_skills(proj = ".", which = NULL, overwrite = FALSE)
}
\arguments{
\item{proj}{Course project root.}

\item{which}{Skill names to install. \code{NULL} installs every shipped skill.}

\item{overwrite}{Whether to replace a skill already installed under \code{proj}.}
}
\value{
The destination directories, invisibly.
}
\description{
Copies each skill directory out of the installed package into
\verb{<proj>/.claude/skills/} and stamps the package version at the bottom of
every copied \code{SKILL.md}.
}
\details{
A skill directory is editable once it is in a course, so an existing
destination stops the call and names what it found. Overwriting is a
deliberate argument, because the alternative is losing a local edit without
anyone seeing it happen.

Nothing is written outside \code{proj}: the destination is built from \code{proj} and
a skill name, and a name this package does not ship stops before any copy.
}
\examples{
root <- init_course(tempfile("course-"), code = "ABCD 101",
                    title = "Demo Course",
                    site_url = "https://example.org/demo",
                    timezone = "America/Chicago", git = FALSE,
                    skills = FALSE)
install_skills(root)
list.files(file.path(root, ".claude", "skills"))
unlink(root, recursive = TRUE)
}
