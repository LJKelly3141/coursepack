% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bank.R
\name{check_question}
\alias{check_question}
\title{Check one question}
\usage{
check_question(q, where)
}
\arguments{
\item{q}{One question, as \code{read_bank()} returns it.}

\item{where}{Location prefix for the error message.}
}
\value{
\code{TRUE}, invisibly, when the question is well formed. Otherwise it
stops.
}
\description{
Refuse the three shapes the generator cannot render: a type it does not
support, fewer than two options, and an answer key naming no option. Every
message is prefixed with \code{where}, so the author is told which item in which
section of which chapter to open.
}
\examples{
q <- list(type = "multiple_choice",
          options = list(A = "the head", B = "the tail"), answer = "A")
check_question(q, "chapter 1: 1.1 Ideas question id 1")

# An answer key naming no option stops, with the location in the message.
q$answer <- "C"
try(check_question(q, "chapter 1: 1.1 Ideas question id 1"))
}
