% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{read_course}
\alias{read_course}
\alias{read_modules}
\alias{read_manifest}
\alias{read_reference}
\title{Read a course's manifests}
\usage{
read_course(proj)

read_modules(proj)

read_manifest(proj)

read_reference(proj)
}
\arguments{
\item{proj}{Course project root.}
}
\value{
\code{read_manifest()} returns \code{list(course, mods, pages, assignments, quizzes)}.
}
\description{
\code{read_course()} and \code{read_modules()} parse \code{course.yml} and \code{modules.yml}
under \code{proj}. \code{read_manifest()} parses both and names the \verb{pages:},
\verb{assignments:} and \verb{quizzes:} definitions by slug or id, stopping on a
duplicate, because every later step indexes by those names.
}
\section{Optional keys these readers pass through}{

Nothing here validates a key it does not use, so a key added for a later
step travels untouched. The ones a course may write today:

\verb{module_id:} on a module, \verb{item_id:} on a module item, and \verb{resource_id:}
on a page, assignment or quiz definition replace the derived Canvas id.
Each must be "g" plus 32 hex; see \code{check_gid()}.

\verb{course_id:} and \verb{manifest_id:} in \code{course.yml} do the same for the two
identifiers a course carries as a whole: the one on \verb{<course>} in
\code{course_settings.xml} and the one on \verb{<manifest>} in \code{imsmanifest.xml}.
Both are otherwise derived from \verb{code:}, and both must be "g" plus 32 hex.
\code{extract_manifest()} writes them, because an export carries the Canvas
course code rather than the \verb{code:} those ids were derived from.

\verb{all_day_date:} on a carried assignment or quiz definition is the local
calendar day Canvas displays for an end-of-day deadline. Without it the
day is the \verb{due:} date's own, which is right for a course whose \verb{due:} is
local and wrong for one whose \verb{due:} is the UTC instant an export stored.
It needs a \verb{due:} beside it and must be \code{YYYY-MM-DD}.

\verb{published:} on a module item. On a header, page or link it is that
item's own state. On an assignment or quiz item it may not claim more
than the definition holds.

\verb{height_measured:} on an iframe page records when \verb{height:} was last
measured. The cartridge builder reads \verb{height:} and ignores this one.

\verb{homework_intro:} and \verb{submission_note:} under \verb{assignment_defaults:} in
\code{course.yml}, or on one assignment definition, are the HTML that sits
above and below the copied directions in an assignment body.
\verb{homework_intro:} fills \code{{href}} and \code{{title}} from the textbook link and
defaults to one neutral sentence; \verb{submission_note:} defaults to empty,
and an empty note appends nothing.

\verb{source:} in \code{reference.yml} names a cartridge whose resources a course
can carry forward, and \verb{source_ref:} on a definition names one of them. A
carried definition needs a \verb{title:} and declares no body key of its own;
a declared source with nothing carried is reported by \code{build_cartridge()}.

\verb{carry:} in \code{course.yml} holds the settings that apply to carried
resources. Its one key today is \verb{repair_html:}, which defaults to \code{true}
and repairs two accessibility defects in the carried bytes on the way into
the cartridge: a \verb{<th>} with no \code{scope}, and a leading bold paragraph used
as a heading. Set it to \code{false} and every carried file travels exactly as
the source wrote it, apart from the title and the due date. Anything other
than \code{true} or \code{false} stops the build.
}

\examples{
root <- init_course(tempfile("course-"), code = "ABCD 101",
                    title = "Demo Course", site_url = "https://example.org/demo",
                    timezone = "America/Chicago", git = FALSE, skills = FALSE)
read_course(root)$code
names(read_modules(root))
m <- read_manifest(root)
names(m)
# The definitions are named by slug or id, ready to index.
names(m$pages)
# reference.yml is optional, and a fresh scaffold carries none.
read_reference(root)
unlink(root, recursive = TRUE)
}
