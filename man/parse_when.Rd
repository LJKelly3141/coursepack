% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/time.R
\name{parse_when}
\alias{parse_when}
\title{Parse a post or due time in one of exactly three forms}
\usage{
parse_when(x, tz, what)
}
\arguments{
\item{x}{The value from YAML.}

\item{tz}{IANA zone.}

\item{what}{A label for the error message (the announcement or assignment id).}
}
\value{
\code{NULL}, or a \code{POSIXct} in \code{tz}.
}
\description{
\code{immediately} (returns \code{NULL}), \code{YYYY-MM-DD} (midnight local), or
\verb{YYYY-MM-DD HH:MM[:SS]}. Anything else stops, because a value that fails to
parse must never quietly become "post now".
}
\examples{
# A bare date is midnight local.
parse_when("2026-01-15", "America/Chicago", "hw-01")
parse_when("2026-01-15 14:30", "America/Chicago", "hw-01")
# `immediately` posts on import, so there is no time to return.
parse_when("immediately", "America/Chicago", "welcome")
}
