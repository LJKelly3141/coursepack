% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate-quiz.R
\name{normalize_tables}
\alias{normalize_tables}
\title{Normalize the tables in a question stem}
\usage{
normalize_tables(html)
}
\arguments{
\item{html}{A question stem, or any HTML fragment.}
}
\value{
The same HTML with every table normalized.
}
\description{
Every \verb{<table>} in \code{html} is given a caption if it has none, \code{scope="col"} on
every unscoped \verb{<th>} of its first row, and a \verb{<thead>} and \verb{<tbody>} split
when that first row is all header cells. A table that already carries those
is returned unchanged, so running this twice changes nothing.
}
\details{
Exported because a bank-authoring session normalizes a stem the same way the
generator will, and comparing the two is how an author sees what the build
will change.
}
