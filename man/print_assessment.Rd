% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/print.R
\name{print_assessment}
\alias{print_assessment}
\title{Print a paper form of an assessment}
\usage{
print_assessment(
  proj = ".",
  id,
  seed,
  out_dir = file.path(proj, "build", "paper"),
  formats = c("docx", "pdf"),
  render = TRUE
)
}
\arguments{
\item{proj}{Course project root.}

\item{id}{A quiz or assignment id from \code{modules.yml}. Quizzes are looked up
first.}

\item{seed}{One whole number. It names the form: the key records it, and the
same seed prints the same paper.}

\item{out_dir}{Where the documents are written. Figures are copied here too.}

\item{formats}{Formats to render, from \code{docx} and \code{pdf}, which are the two
the front matter declares.}

\item{render}{Render with quarto when it is on the PATH. With \code{FALSE}, or
with no quarto, the documents are written and where they are is printed.}
}
\value{
\code{invisible(list(qmd, key, rendered))}. \code{key} is \code{NULL} for an
assignment, which has no answer key. \code{rendered} is the form's rendered
files, named by format, and is empty when nothing was rendered; a key is
rendered to the same formats beside its form.
}
\description{
Write one fixed form of an assessment as a Quarto document, and, for a quiz
drawn from a question bank, the answer key that goes with it.
}
\details{
A \verb{bank:} quiz is a draw: \code{set.seed(seed)}, then one sample without
replacement from each question group, in group order. The same seed produces
the same paper, because nothing written here carries a timestamp; \code{date: today} in the front matter is a Quarto keyword resolved at render. Two runs
of one seed are byte-identical files, and two forms differ only where their
questions do.

Question stems are HTML in a bank, so they are converted to Markdown with
pandoc: tables become pipe tables, and a figure is copied beside the document
and referenced by its bare filename with the bank's alt text. Options are
listed \code{A.} to \code{D.} in the order the bank wrote them, and the key records the
letter as printed.

The printable forms are a quiz declaring \verb{bank:} and an assignment declaring
\verb{description:} or \verb{homework:}. A \verb{qti:} quiz, a \verb{quiz_file:} assignment and
any carried \verb{source_ref:} definition keep their student-facing text outside
this package, so they are refused rather than printed with the questions
missing.
}
