% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a11y.R
\name{audit_course}
\alias{audit_course}
\title{Audit a course's rendered output against WCAG 2.1 AA}
\usage{
audit_course(
  repos,
  proj = ".",
  out_dir = file.path(proj, "project", "accessibility"),
  port = 8766L,
  cartridge_dirs = c("build/coursepack", "reference"),
  video_fetch = default_youtube_fetch
)
}
\arguments{
\item{repos}{Character vector of repository paths to audit. Required; no
repository is assumed and an empty vector stops.}

\item{proj}{The course project root. Used only to place \code{out_dir}.}

\item{out_dir}{Where the three files are written.}

\item{port}{The port the local server binds while pa11y reads a page.}

\item{cartridge_dirs}{Directories under each repository to search for
cartridges, passed to \code{\link[=cartridges_in]{cartridges_in()}}.}

\item{video_fetch}{The watch-page fetcher passed to \code{\link[=audit_cartridge]{audit_cartridge()}}.
Defaults to the real network fetcher.}
}
\value{
Invisibly, a list with \code{findings} (the full frame, classified rows
plus the notice rows), \code{meta} (the run's metadata) and \code{files} (the three
paths written).
}
\description{
Serves each repository's rendered output over a local HTTP server, runs
pa11y across every page, layers the custom source checks on top, audits any
Common Cartridge found under the repository, classifies each finding by the
kind of fix it needs, and writes a JSON record, a findings document and a
remediation plan.
}
\details{
The measured page count and the declared scope are accumulated on two
separate code paths on purpose, so the report's coverage comparison is a
real cross-check rather than a value compared against itself. The comments
in this file say why.
}
