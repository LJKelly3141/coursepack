% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init.R
\name{render_template}
\alias{render_template}
\title{Fill a template's placeholders}
\usage{
render_template(text, values)
}
\arguments{
\item{text}{The template, as one string or as lines to be joined with
newlines. An empty file renders to an empty string rather than stopping.}

\item{values}{A named list of replacements. Each is coerced with
\code{as.character()}; \code{NULL} renders as an empty string.}
}
\value{
The rendered text, as one string.
}
\description{
Replace every \code{{{key}}} in \code{text} with the matching entry of \code{values}, and
refuse to return text that still holds a placeholder.
}
\details{
Substitution is literal, not a regular expression, so a value containing a
backslash or a dollar sign travels unchanged. A key \code{values} does not carry
is not silently left alone: the leftover check names it and stops.
}
