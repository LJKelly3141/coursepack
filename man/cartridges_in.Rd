% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a11y-cartridge.R
\name{cartridges_in}
\alias{cartridges_in}
\title{Every cartridge under a repository's conventional locations}
\usage{
cartridges_in(repo, dirs = CARTRIDGE_SEARCH_DIRS)
}
\arguments{
\item{repo}{The repository path.}

\item{dirs}{Directories under \code{repo} to search, relative. Defaults to the
conventional pair; an argument so a caller with a differently shaped
repository is not forced to move its files.}
}
\value{
Full paths to every \code{.imscc} found, possibly none.
}
\description{
The one place the search directories are read, so the audit driver and the
declaration in \code{\link[=intended_surfaces]{intended_surfaces()}} can never look in different places.
}
