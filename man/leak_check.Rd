% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init.R
\name{leak_check}
\alias{leak_check}
\title{Refuse to publish an answer key}
\usage{
leak_check(proj = ".", docs = "docs")
}
\arguments{
\item{proj}{Course project root.}

\item{docs}{The published directory, relative to \code{proj} or absolute. A
missing directory stops: nothing was scanned, so nothing is known.}
}
\value{
\code{invisible(character(0))} when clean. Otherwise it stops, naming
every file that carries the canary.
}
\description{
Read every byte of everything under \verb{<proj>/<docs>} looking for the leak
canary, and stop when it is found.
}
\details{
\verb{docs/} is what GitHub Pages serves, so everything in it is public and
everything outside it is not. The canary is a string that lives in
\verb{assessments/} and nowhere else; finding it under \verb{docs/} means the render
allowlist let assessment content through.

This is one of three independent guards on the same failure, and none of them
covers the others: the quiz body extractor truncates at the answer-key
heading, it stops if the canary survives that truncation, and
\code{\link[=build_cartridge]{build_cartridge()}} scans its whole staging tree before it zips. This one
guards the web site, which the other two never touch.
}
\examples{
root <- init_course(tempfile("course-"), code = "ABCD 101",
                    title = "Demo Course",
                    site_url = "https://example.org/demo",
                    timezone = "America/Chicago", git = FALSE,
                    skills = FALSE)
# Stand in for a rendered site: docs/ is what GitHub Pages serves.
dir.create(file.path(root, "docs"))
writeLines("<p>A rendered page.</p>", file.path(root, "docs", "index.html"))
leak_check(root)
unlink(root, recursive = TRUE)
}
