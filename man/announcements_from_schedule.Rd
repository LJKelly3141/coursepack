% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/announcements-schedule.R
\name{announcements_from_schedule}
\alias{announcements_from_schedule}
\title{Weekly announcements from a schedule}
\usage{
announcements_from_schedule(
  proj = ".",
  schedule = "schedule.yml",
  out_dir = "content/announcements",
  release_time = "07:00",
  calendar_url = NULL
)
}
\arguments{
\item{proj}{Course project root.}

\item{schedule}{The schedule file, relative to \code{proj} or absolute.}

\item{out_dir}{Where the bodies are written, relative to \code{proj}. Also the
\verb{body_dir:} of the declaration.}

\item{release_time}{\code{"HH:MM"} in the course zone; every week posts at that
time on its Monday.}

\item{calendar_url}{A link to the course calendar for the closing paragraph.
\code{NULL} writes the paragraph without it.}
}
\value{
Invisibly, a data frame with one row per week: \code{id}, \code{title}, \code{post}
and \code{body}, the four fields the declaration carries.
}
\description{
Turn a \code{schedule.yml} into one announcement per week: an HTML body per week
under \code{out_dir} and an \code{announcements.yml} declaring them, which is what
\code{\link[=build_cartridge]{build_cartridge()}} already reads. Nothing here touches the cartridge.
}
\details{
Each week is one entry under \verb{weeks:}. \verb{monday:} is that week's Monday and
must be one. \verb{module:} names the module the week covers and \verb{label:}
overrides the counted \verb{Week N}. \verb{notes:} is a sentence for the top. Any of
\verb{tuesday:} to \verb{sunday:} is a class day, written as a mapping with \verb{class:} or
\verb{holiday:}, and optionally \verb{assigned:} and \verb{prepare:}. The body carries one
\verb{<h3>} block per class day, in weekday order, and then what is due by Sunday.

Due items are the manifest's own assignments and quizzes that declare a
\verb{due:}. An item due Monday to Thursday is listed under its own day; Friday,
Saturday and Sunday collect into the closing \verb{Due by Sunday} list, because
that is the deadline a student is working to over the weekend. An item due on
a weekday the schedule does not declare still gets a heading of its own,
marked as no class meeting, rather than being dropped. A title containing
\code{Exam} is labelled \code{Exam} rather than \code{Due}.

An existing \code{announcements.yml} is never overwritten. The declaration is
written to \code{announcements.generated.yml} instead and the function says so, so
a course that has edited its announcements by hand can merge rather than
discover the loss later.
}
