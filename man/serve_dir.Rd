% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a11y-serve.R
\name{serve_dir}
\alias{serve_dir}
\title{Serve a directory over HTTP for the audit}
\usage{
serve_dir(dir, port = 8766L)
}
\arguments{
\item{dir}{Directory to serve. Must exist.}

\item{port}{Port to listen on.}
}
\value{
A list of \code{url}, \code{port}, \code{pid}, and \code{stop}, a function that kills
the server this call spawned. Call \code{stop()} yourself; nothing else will.
}
\description{
Chrome gives every \verb{file://} document a unique opaque origin, so a headless
audit of \verb{file://} URLs sees a different document than a student does, and
framed content does not load at all. The audit serves the rendered pages
over HTTP, audits them, and tears the server down.
}
\details{
Refuses to attach to a port something is already answering on: a leaked
server from an earlier call would return valid JSON for the wrong
directory, which reads as a clean audit of content nobody asked for.
}
\examples{
\dontrun{
# Needs python3, which serves the directory, and a free port.
pages <- tempfile("site-")
dir.create(pages)
writeLines("<html lang='en'><title>Week 1</title></html>",
           file.path(pages, "index.html"))

srv <- serve_dir(pages)
srv$url
srv$stop()          # nothing else stops it for you

unlink(pages, recursive = TRUE)
}
}
