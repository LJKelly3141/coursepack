% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a11y-serve.R
\name{pa11y_raw}
\alias{pa11y_raw}
\title{Audit one URL with pa11y}
\usage{
pa11y_raw(url, timeout_s = 120L)
}
\arguments{
\item{url}{URL to audit. Serve the pages first; see \code{\link[=serve_dir]{serve_dir()}}.}

\item{timeout_s}{Per-page timeout, in seconds.}
}
\value{
The parsed pa11y issue list, one element per issue.
}
\description{
Runs pa11y over one URL and returns its parsed issue list. Both runners
are named explicitly, and warnings and notices are included: pa11y's JSON
reporter drops every issue that is not type \code{error} without them, which is
most of what a WCAG AA audit is for.
}
\details{
Two failures are distinguished, because they are different realities. A
tool that never produced output raises a \code{pa11y_unavailable} condition and
a caller may skip on it; a tool that ran and wrote something that is not
valid JSON raises \code{pa11y_broken} and must never read as "not run".
}
