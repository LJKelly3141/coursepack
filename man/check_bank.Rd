% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bank.R
\name{check_bank}
\alias{check_bank}
\title{Check a whole question bank}
\usage{
check_bank(bank, images_dir)
}
\arguments{
\item{bank}{A bank, as \code{read_bank()} returns it.}

\item{images_dir}{Directory holding the bank's figure PNGs.}
}
\value{
\verb{list(warnings = <character>)}, one entry per figure with no alt
text, each naming the chapter, the section and the question id.
}
\description{
Every question is checked, ids are required to be unique within the chapter,
and every question naming an \code{image} must have that file under \code{images_dir}.
A figure carrying no \code{alt} text is returned as a warning rather than stopping
the run, because the item still renders.
}
\details{
Exported so a bank-authoring session can check its own work without building
anything.
}
