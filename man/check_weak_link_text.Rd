% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a11y-cartridge.R
\name{check_weak_link_text}
\alias{check_weak_link_text}
\title{Weblink text that says nothing out of context}
\usage{
check_weak_link_text(xml_files, surface)
}
\arguments{
\item{xml_files}{Paths to the cartridge's XML files. Anything that is not a
\verb{<webLink>} resource is skipped.}

\item{surface}{The caller's course-of-origin label. Required, for the same
reason \code{\link[=check_untitled_iframes]{check_untitled_iframes()}} requires it.}
}
\value{
A list of one-row findings, empty when every link reads well.
}
\description{
"Click here", a bare URL, and an empty \verb{<title></title>} all fail WCAG 2.4.4
when read out of context, which is how a screen reader user navigates a link
list. Reads the cartridge's \verb{<webLink>} resource files.
}
\examples{
# One weblink resource, shaped the way a Common Cartridge writes them.
link <- tempfile(fileext = ".xml")
writeLines(c(
  "<webLink xmlns='http://www.imsglobal.org/xsd/imswl_v1p1'>",
  "  <title>click here</title>",
  "  <url href='https://example.org/reading' />",
  "</webLink>"), link)

found <- check_weak_link_text(link, surface = "demo-cartridge")
length(found)
found[[1]]$issue

unlink(link)
}
