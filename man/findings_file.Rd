% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/a11y-model.R
\name{findings_file}
\alias{findings_file}
\alias{write_findings}
\alias{read_findings}
\title{Write and read a findings file}
\usage{
write_findings(df, meta, path)

read_findings(path)
}
\arguments{
\item{df}{A data frame of \code{\link[=finding]{finding()}} rows.}

\item{meta}{The run's metadata list, written under \code{audit}.}

\item{path}{The JSON file to write or read.}
}
\value{
\code{write_findings()} returns \code{path} invisibly; \code{read_findings()}
returns a data frame with the \code{FINDING_COLS} columns.
}
\description{
The JSON contract between one audit run and the next. \code{write_findings()}
writes the run's metadata and its findings; \code{read_findings()} reads them
back with every \code{FINDING_COLS} column present and typed, including on a
zero-row file, which is the state a course is trying to reach and not an
edge case to crash on.
}
\details{
jsonlite cannot infer a type from an all-null JSON array, so a column that
is NA on every row comes back logical rather than character or integer.
\code{read_findings()} restores the types rather than letting a silent flip
reach a later stage that writes into those columns.
}
\examples{
two <- rbind(
  finding("demo", "chapter1.html", "1.1.1", "A", "image has no alt text",
          "img[src='fig/a.png']", "serious"),
  finding("demo", "chapter2.html", "2.4.4", "A",
          "link text does not describe its destination", "a[href='/next']",
          "moderate"))

path <- tempfile(fileext = ".json")
write_findings(two, meta = list(date = "2026-09-05"), path)
back <- read_findings(path)
nrow(back)
identical(back$id, two$id)   # every id survives the round trip
vapply(back[, c("line", "fix_class")], class, character(1))

unlink(path)
}
