% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/resolve.R
\name{resolve_target}
\alias{resolve_target}
\title{Resolve a textbook chapter and anchor}
\usage{
resolve_target(chapter, anchor, tb_docs)
}
\arguments{
\item{chapter}{A chapter slug, \code{NULL}, or an empty string.}

\item{anchor}{An optional anchor name.}

\item{tb_docs}{Path to the rendered textbook directory.}
}
\value{
A list with \code{state} and \code{detail}. \code{state} is one of \code{ok},
\code{missing-chapter}, \code{missing-anchor}, or \code{unchecked}.
}
\description{
Reports \code{ok} when the chapter and requested anchor exist,
\code{missing-chapter} when the chapter file is absent, \code{missing-anchor} when the
file lacks the requested anchor, and \code{unchecked} when resolution cannot be
performed. The two fail-open scars are an absent textbook directory and a
\code{NULL} or empty chapter; both return \code{unchecked}, even when the other input
would permit a check.
}
\examples{
docs <- file.path(tempdir(), "book-docs")
dir.create(docs, showWarnings = FALSE)
writeLines('<h2 id="sec-mean">The mean</h2>', file.path(docs, "ch01.html"))
resolve_target("ch01", NULL, docs)$state
resolve_target("ch01", "sec-mean", docs)$state
resolve_target("ch01", "sec-nope", docs)
resolve_target("ch99", NULL, docs)$state
# The two fail-open scars: no chapter to check, and no textbook to check it in.
resolve_target(NULL, NULL, docs)$state
resolve_target("ch01", NULL, file.path(tempdir(), "no-such-book"))$state
unlink(docs, recursive = TRUE)
}
